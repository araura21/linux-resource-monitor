# linux-resource-monitor
Mini Resource Monitor for Linux developed in Python. Displays CPU, memory, processes, disk, network and user information using the /proc filesystem, concurrent programming (fork and threads), and CRUD persistence.
